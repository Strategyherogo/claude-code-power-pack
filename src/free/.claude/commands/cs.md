# Skill: cs (Session Start)
Quick session briefing — where you left off.

## Auto-Trigger
**When:** "cs", "start session", "what's going on", "where was I"

## Workflow

Run ONE command:
```
bash $HOME/ClaudeCodeWorkspace/.claude/scripts/cs-briefing.sh
```

Display the output as-is. Then: "What's next?"

## Rules
- Single Bash call only — the script handles everything internally
- No subagents, no extra tool calls
- No tables, no headers, no suggested priorities list
- Display script output verbatim, don't reformat

---
*Last updated: 2026-02-26*
